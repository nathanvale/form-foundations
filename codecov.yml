codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  status:
    project:
      default: off
      atoms:
        flags: atoms
      core:
        flags: core
      examples:
        flags: examples
      normalize:
        flags: normalize
      widgets:
        flags: widgets

  flags:
    atoms:
      paths:
        - packages/atoms/src
    core:
      paths:
        - packages/core/src
    examples:
      paths:
        - packages/examples/src
    normalize:
      paths:
        - packages/normalize/src
    widgets:
      paths:
        - packages/widgets/src

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no

comment:
  layout: 'header, diff'
  behavior: default
  require_changes: no
